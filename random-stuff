" <CR>: close popup and save indent.
inoremap <silent> <CR> <C-r>=<SID>my_cr_function()<CR>
function! s:my_cr_function()
  return deoplete#mappings#smart_close_popup() . "\<CR>"
endfunction


# pachages to install on new linux machine
tmux clang llvm neovim fish zsh tree ruby gem stack haskell-platform cmake 
python-devel python3-develautomake gcc gcc-c++ kernel-devel

# pip install
sudo pip install --upgrade neovim
sudo pip install vim-vint cmakelint yamllint

# gnome specific
gconf-editor gnome-tweak-tool
